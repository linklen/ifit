<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/login_regist">

    <Button
        android:id="@+id/no_user_registered"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/user_login_button"
        android:layout_below="@+id/user_login_button"
        android:text="没有账号，点此注册"
        android:background="#00000000"
        android:textSize="10sp" />

    <EditText
        android:id="@+id/user_password"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/user_login_button"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/userpic"
        android:ems="10"
        android:hint="请输入密码"
        android:inputType="textPassword" />

    <ImageView
        android:id="@+id/userpic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/keypic"
        android:layout_alignTop="@+id/user_name"
        android:src="@drawable/user" />

    <Button
        android:id="@+id/user_login_button"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/user_password"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:background="@drawable/shape"
        android:text="@string/login" />

    <ImageView
        android:id="@+id/keypic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/user_login_button"
        android:layout_alignLeft="@+id/user_login_button"
        android:src="@drawable/key" />

    <EditText
        android:id="@+id/user_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/user_password"
        android:layout_alignRight="@+id/user_password"
        android:layout_toRightOf="@+id/keypic"
        android:ems="10"
        android:hint="请输入用户名" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/forget_password"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/user_login_button"
        android:layout_below="@+id/user_login_button"
        android:text="@string/forgotpassword"
        android:textSize="10sp"
        android:background="#00000000" />

</RelativeLayout>
